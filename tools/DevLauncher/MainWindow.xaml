<Window x:Class="DevLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:models="clr-namespace:DevLauncher.Models"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="LogAllTheThings Dev Launcher"
        Width="1060"
        Height="720"
        MinWidth="820"
        MinHeight="560"
        Background="#111418"
        Foreground="#F5F7FA"
        Closing="Window_Closing">
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#1B2026" />
        <SolidColorBrush x:Key="BorderBrush" Color="#303842" />
        <Style TargetType="Button">
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Background" Value="#2D6CDF" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,20">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock FontSize="25" FontWeight="SemiBold" Text="Dev Launcher" />
                <TextBlock Margin="0,4,0,0" Foreground="#97A3B1" Text="LogAllTheThings local services" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Right" HorizontalAlignment="Right" Orientation="Horizontal">
                <Button Command="{Binding StartAllCommand}" Content="Start all" />
                <Button Command="{Binding StopAllCommand}" Background="#B64242" Content="Stop all" />
                <Button Command="{Binding RestartAllCommand}" Background="#59636F" Content="Restart all" />
                <Button Command="{Binding ForceRestartAllCommand}" Background="#C0392B" Content="Force restart all" Margin="0" />
            </StackPanel>
        </DockPanel>

        <Border x:Name="ErrorBanner" Grid.Row="1" Margin="0,0,0,16" Padding="12,8"
            Background="#4A2227" BorderBrush="#A84D58" BorderThickness="1"
            CornerRadius="4" Visibility="Collapsed">
            <DockPanel>
            <Button DockPanel.Dock="Right" Click="DismissError_Click" Margin="12,0,0,0"
                Padding="10,4" Background="#71343C" Content="Dismiss" />
            <TextBlock x:Name="ErrorText" VerticalAlignment="Center" Foreground="#FFD8DC"
                   TextWrapping="Wrap" />
            </DockPanel>
        </Border>

        <ItemsControl Grid.Row="2" ItemsSource="{Binding Services}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="1" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Margin="0,0,12,18" Padding="18" Background="{StaticResource PanelBrush}"
                            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="6">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <DockPanel>
                                <TextBlock FontSize="18" FontWeight="SemiBold" Text="{Binding Name}" />
                                <Border DockPanel.Dock="Right" Padding="8,3" Background="#29313A" CornerRadius="3">
                                    <TextBlock Foreground="#9FC0FF" Text="{Binding StateText}" />
                                </Border>
                            </DockPanel>
                            <StackPanel Grid.Row="1" Margin="0,10,0,14" Orientation="Horizontal">
                                <TextBlock Foreground="#AAB4BF" Text="{Binding Address}" />
                                <TextBlock Margin="14,0,0,0" Foreground="#6F7B87" Text="{Binding ProcessText}" />
                            </StackPanel>
                            <StackPanel Grid.Row="2" Orientation="Horizontal">
                                <Button Command="{Binding StartCommand}" Content="Start" />
                                <Button Command="{Binding StopCommand}" Background="#B64242" Content="Stop" />
                                <Button Command="{Binding RestartCommand}" Background="#59636F" Content="Restart" />
                                <Button Command="{Binding ForceRestartCommand}" Background="#C0392B" Content="Force restart" Margin="0" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <TabControl x:Name="MainTabs" Grid.Row="3" Grid.RowSpan="2" Background="Transparent"
                    BorderBrush="{StaticResource BorderBrush}" Foreground="#F5F7FA">
            <TabItem Header="Logs">
                <Grid Background="#111418">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <DockPanel Margin="0,10,0,10">
                        <TextBlock VerticalAlignment="Center" FontSize="15" FontWeight="SemiBold" Text="Output" />
                        <StackPanel DockPanel.Dock="Right" HorizontalAlignment="Right" Orientation="Horizontal">
                            <CheckBox x:Name="AutoScrollCheckBox" VerticalAlignment="Center" Margin="0,0,16,0"
                                      Foreground="#C9D1D9" Content="Auto-scroll" IsChecked="True" />
                            <Button Click="OpenLog_Click" Background="#59636F" Content="Open log file" />
                            <Button Click="ClearLogs_Click" Background="#59636F" Content="Clear" Margin="0" />
                        </StackPanel>
                    </DockPanel>

                    <Border Grid.Row="1" Background="#0B0E11" BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1" CornerRadius="4">
                        <ListBox x:Name="LogList" ItemsSource="{Binding Logs}" Background="Transparent"
                                 BorderThickness="0" FontFamily="Cascadia Mono, Consolas" FontSize="12"
                                 ScrollViewer.CanContentScroll="True"
                                 VirtualizingPanel.IsVirtualizing="True"
                                 VirtualizingPanel.VirtualizationMode="Recycling">
                            <ListBox.ItemTemplate>
                                <DataTemplate DataType="{x:Type models:LogLine}">
                                    <TextBlock Padding="12,4" Text="{Binding Display}" TextWrapping="Wrap">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#C9D1D9" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsError}" Value="True">
                                                        <Setter Property="Foreground" Value="#FF8A8A" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem x:Name="AppTab" Header="App">
                <Grid Background="#111418">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <DockPanel Margin="0,10">
                        <TextBlock VerticalAlignment="Center" Foreground="#AAB4BF"
                                   Text="http://localhost:5173" />
                        <StackPanel DockPanel.Dock="Right" HorizontalAlignment="Right" Orientation="Horizontal">
                            <Button Click="RefreshApp_Click" Background="#59636F" Content="Refresh" />
                            <Button Click="OpenApp_Click" Background="#59636F" Content="Open in browser" Margin="0" />
                        </StackPanel>
                    </DockPanel>

                    <Border Grid.Row="1" Background="#FFFFFF" BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1" CornerRadius="4" ClipToBounds="True">
                        <Grid>
                            <wv2:WebView2 x:Name="AppWebView" Visibility="Collapsed" />
                            <Border x:Name="AppWaitingPanel" Background="#151A20">
                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock HorizontalAlignment="Center" FontSize="20" FontWeight="SemiBold"
                                               Foreground="#F5F7FA" Text="App preview" />
                                    <TextBlock x:Name="AppWaitingText" Width="420" Margin="0,10,0,0"
                                               Foreground="#97A3B1" TextAlignment="Center" TextWrapping="Wrap"
                                               Text="Start the Web service to load the app here." />
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
